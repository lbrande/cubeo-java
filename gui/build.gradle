jar {
    manifest {
        attributes(
                'Class-Path': 'se.lovebrandefelt.cubeo.gui',
                'Main-Class': 'se.lovebrandefelt.cubeo.gui.GUI',
        )
    }
    from configurations.compileClasspath.collect { it.isDirectory() ? it : zipTree(it) }
}